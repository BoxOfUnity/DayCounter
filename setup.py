from setuptools import setup, find_packages

setup(
    name = "discord.py",
    version = "1.7.3",
    description = "",
    packages = find_packages("discord.py"),
    install_requires = ['discord.py']  # Example of external package
)
